%DOCTYPE
%html
  %head
    = javascript_pack_tag "app/assets/javascript/packs/Root"
    %title
      = yield(:title) || "The blog"

  = render partial: "shared/down"
  -# = render partial: "shared/navbar"
%div{:id=>"modal-holder"}

%title
  = yield(:title)

%div{:id=>"root"}
  = yield
