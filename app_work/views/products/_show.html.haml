
%div{:class=>"row"}
  = render "store/modal_for_cart"
  -if @resource .present?  
    %div{:class=>"col-md-6"}     
      %p{:class=>"tovar"}
        = @resource.title.capitalize
      %p{:class=>"link_different"}

        = ui_button 'back',  t(:back), products_path
        -if @resource.present? && user_signed_in?
          -if can_manage(current_user.products, @resource, Product)
            = ui_button 'edit',  t(:edit), edit_product_path(@resource), data: { modal: true }  
            = ui_button 'delete',  t(:delete), product_path(@resource), method: :delete

      %p
        =  link_to image_tag(@resource.uploaded_file.url, size: "400x300"), product_path(@resource)
      
      %div{:class=>"like_dislike_id"}
                
        =render partial: "voices/like_dislike", locals: {resourse_number: @resource, name_controller: Product} 
                             
      %div{:class=>"row"}


        %div{:class=>"col-md-5"} 
          %p{:class=>"price_show"}
            Price:
            = @resource.price
        %div{:class=>"col-md-5"}     
  
          
    %div{:class=>"col-md-6"}     
      %p
        %b Description:
        -if params[:action]=='show'   
          =@resource.description
          = render partial: "products/modal_of_description"    
        -else  
          = @resource.description
          = render partial: "products/modal_of_description"  
         

        %p{:class=>"buy_product"}
        
        = button_to 'buy', line_items_path(product_id: @resource.id),remote: true, class: "btn btn-success"  
       

  -else
    =image_tag("/assets/error.gif", width: '300', height: '200', alt: 'Site Logo', class: "logo") 
